<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 14px;
            line-height: 1.6;
            color: #333;
            margin: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        .header h1 {
            font-size: 28px;
            font-weight: bold;
            color: #0055a5;
            margin: 0;
        }
        .header h2 {
            font-size: 22px;
            font-weight: bold;
            color: #000;
            margin: 5px 0;
        }
        .contact-info {
            margin-bottom: 20px;
        }
        .contact-info p {
            margin: 5px 0;
        }
        .recipient-info {
            margin-bottom: 20px;
        }
        .intro {
            font-weight: bold;
            margin-top: 20px;
        }
        .content {
            margin-top: 20px;
        }
        .footer {
            margin-top: 30px;
            padding-top: 10px;
            border-top: 1px solid #ddd;
            font-size: 12px;
            color: #777;
        }
        @media print {
            body {
                margin: 0;
                padding: 0;
            }
            .header, .footer {
                border: none;
            }
        }
    </style>
    <title>Cover Letter - {{ user_name }}</title>
</head>
<body>
    <div class="header">
        <h1>Cover Letter</h1>
        <h2>{{ user_name }}</h2>
    </div>

    <div class="contact-info">
        <p>{{ user_address }}</p>
        <p>{{ user_phone }}</p>
        <p>{{ user_email }}</p>
        <p>{{ current_date }}</p>
    </div>

    <div class="recipient-info">
        <p>{{ hiring_manager_name | default("Hiring Team", true) }}</p>
        <p>{{ company_name }}</p>
        <p>{{ company_address }}</p>
    </div>

    <div class="content">
        <p class="intro">Dear {{ hiring_manager_name | default("Hiring Team", true) }},</p>
        <p>{{ cover_letter_text | replace("\n", "<br>") }}</p>
    </div>

</body>
</html>